# Temporary fork-PR CI proof

This file exists only to give a throwaway fork PR a non-empty diff so it can
be opened, for the sole purpose of observing task-10's `engine_backed` job
behave correctly on a real fork PR (no repository secrets available). This
PR will be closed without merging and this file deleted.
